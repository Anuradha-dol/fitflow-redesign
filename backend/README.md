# FitFlow Core API

Selected backend: **NestJS (TypeScript)**.

The core API is responsible for request validation, authorization/RBAC, transactions, REST APIs, WebSocket integration, and coordination with PostgreSQL, Redis, storage, and the AI service.

This starter exposes:

- `GET /health` — simple API health check
